{
  "name": "eslint-config-vi",
  "version": "4.0.0",
  "description": "ESLint shareable config used by VI Company",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/vicompany/eslint-config-vi.git"
  },
  "scripts": {
    "find-rules": "eslint-find-rules -u -n ./node.js",
    "test": "eslint test/**/*.js",
    "preversion": "npm test",
    "postversion": "git push && git push --tags"
  },
  "author": "VI Company",
  "license": "MIT",
  "keywords": [
    "eslint",
    "eslintconfig",
    "vi-company"
  ],
  "engines": {
    "node": ">=4"
  },
  "devDependencies": {
    "eslint": "^3.15.0",
    "eslint-find-rules": "^1.14.3"
  },
  "peerDependencies": {
    "eslint": ">=3"
  }
}
