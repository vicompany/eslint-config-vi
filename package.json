{
  "name": "eslint-config-vi",
  "version": "0.6.3",
  "description": "ESLint shareable config used by VI Company",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/vicompany/eslint-config-vi.git"
  },
  "scripts": {
    "test": "eslint -c ./index.js test/es6/*.js && eslint -c ./legacy.js test/legacy/*.js",
    "preversion": "npm test",
    "postversion": "git push && git push --tags"
  },
  "author": "VI Company",
  "license": "MIT",
  "keywords": [
    "eslint",
    "eslintconfig",
    "vi-company"
  ],
  "devDependencies": {
    "eslint": "^2.13.1"
  }
}
